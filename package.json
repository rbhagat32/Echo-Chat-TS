{
  "name": "echo-chat-ts",
  "scripts": {
    "startOld": "npm stop && cls && docker compose up --watch",
    "start": "npm stop && cls && docker compose watch",
    "stop": "docker compose down"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rbhagat32/Echo-Chat-TS.git"
  }
}
